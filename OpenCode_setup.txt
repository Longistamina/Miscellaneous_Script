# 1. Install opencode:
curl -fsSL https://opencode.ai/install | bash

# 2. Refresh shel
source ~/.bashrc

# 3. Install Ollama and pull model (example: gemma4:31b)
curl -fsSL https://ollama.com/install.sh | sh
ollama pull gemma4:31b

# 4. cd to the project directory and launch opencode (use ollama to launch)
cd /path/to/project
ollama launch opencode

# 5. Select the downloaded model (gemma4:31b) then Enter to launch opencode TUI (terminal user interface)

#-------------------------------------------------#
#----------------- Other guides ------------------#
#-------------------------------------------------#

#############################
## Enter to newline in TUI ##
#############################

Press Alt+Enter

####################
## Copy-Cut-Paste ##
####################

# Deactivate the "copy on highlight"
## Method 1:
export OPENCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT=1
source ~/.bashrc

## Method 2:
Open your global config file (usually located at ~/.config/opencode/opencode.json)
add the setting under the tui block:
{
  "tui": {
    "copy_on_select": false
  }
}

# Then, use Ctrl+C/X/V like normal (or try Ctrl+Shift+C/X/V)

########################
## Select old session ##
########################

Insie opencode TUI, type "/sessions" and choose the session related to the old work you want to continue
